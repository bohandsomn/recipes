import { SaveTokenInterceptor } from './save-token.interceptor'

describe('SaveTokenInterceptor', () => {
    it('should be defined', () => {
        expect(new SaveTokenInterceptor()).toBeDefined()
    })
})
