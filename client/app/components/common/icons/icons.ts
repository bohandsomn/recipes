import { createIcon } from '@/utils'
import { accountIconProps, searchIconProps, wishIconProps, logInIconProps } from './constants'

export const SearchIcon = createIcon(searchIconProps)
export const WishIcon = createIcon(wishIconProps)
export const AccountIcon = createIcon(accountIconProps)
export const LogInIcon = createIcon(logInIconProps)
