import React from 'react'
import { WishIcon } from '@/components/common'

export const WishRecipe = () => {
    return (
        <WishIcon width={24} hanging={24} fill="#fff" stroke={'transparent'} className="cursor-pointer" />
    )
}
